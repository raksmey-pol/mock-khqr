services:
  mock-store-web:
    build:
      context: ./mock-store-web
      dockerfile: Dockerfile
    image: mock-store-web:latest
    container_name: mock-store-web
    restart: unless-stopped
    env_file:
      - ./mock-store-web/.env
    environment:
      - NODE_ENV=production
      - HOSTNAME=0.0.0.0
      - PORT=4000
    ports:
      - "127.0.0.1:4000:4000"
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:4000/store/health"]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 20s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
